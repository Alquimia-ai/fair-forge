# Fair Forge Test Runner Configuration

# Storage Backend (local or lakefs)
# local: Store test files and results on local filesystem (default, easier for development)
# lakefs: Store in LakeFS for version control and collaboration
TEST_STORAGE_BACKEND=local

# Test Configuration
# TEST_SUITES_TO_RUN: Comma-separated list of test suites to run (leave empty to run all)
TEST_SUITES_TO_RUN=prompt_injection,toxicity
# TEST_RESULT_LANGUAGE: Language for test datasets (default: english)
TEST_RESULT_LANGUAGE=english

# Agent Configuration (required)
ALQUIMIA_API_KEY=alquimia_api_key
ALQUIMIA_URL=alquimia_urk
AGENT_ID=agent_id
CHANNEL_ID=channel_id

# LakeFS Configuration (only required if TEST_STORAGE_BACKEND=lakefs)
LAKEFS_HOST=lakefs_host
LAKEFS_USERNAME=lakefs_username
LAKEFS_PASSWORD=lakefs_password
LAKEFS_REPO_ID=lakefs_repo_id
